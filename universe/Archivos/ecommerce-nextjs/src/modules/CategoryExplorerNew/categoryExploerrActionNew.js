import { LOAD_CONFIG_APP } from './appTypes'

export const increment = () => (dispatch) => {
  dispatch({
    type: 'INCREMENT'
  })
}

export const increment = () => (dispatch) => {
  dispatch({
    type: 'DECREMENT'
  })
}
